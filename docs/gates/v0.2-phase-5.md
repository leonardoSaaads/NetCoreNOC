# v0.2 Gate 5 — Release (P1)

Date: 2026-07-20. Verdict: **PASS**.

## Release artifacts

- `MIGRATION.md` — finalized upgrade path and legacy-token deprecation timeline.
- `SECURITY.md` — operator guide: recommended deploy, roles, TLS options, audit backup +
  periodic `audit verify`, what the banners mean, data-at-rest notes.
- `CHANGELOG.md` — `[0.2.0]` entry (Added / Fixed / Changed).
- `docs/SECURITY-REVIEW-0.2.md` — F1–F6 finding → fix commit → regression test map.
- `docs/BUILD-REPORT-0.2.md` — what changed, quality/perf numbers, decisions, deferred.
- `README.md` — v0.2.0 login/roles/deploy, updated env table, security section.
- CI gains an explicit secret-leak scan step; `pyproject`/`__init__` at 0.2.0.

## Final QA

```
ruff check + ruff format --check : clean
mypy --strict                    : no issues in 39 source files
pytest                           : 171 passed
coverage on opticorr/            : 94.63% (gate >= 85%)
bandit -c pyproject.toml         : 0 issues
pip-audit                        : no known vulnerabilities
```

## Quickstart verified from scratch (real server + real UDP)

Booted `python -m opticorr.main` on a fresh DB (unprivileged ports), zero configuration:

- Server started; bootstrap admin password printed **once** to the console.
- Replayed the bundled fiber-cut scenario as genuine SNMPv2c trap PDUs over UDP.
- Signed in (forced password change on first login), then `/api/stats` reported **8 active
  alarms** and `/api/situations` reported **1 open situation of 8 alarms** — the correlator
  works end-to-end through the full authenticated v0.2.0 stack.

## Upgrade verified from a real v0.1.0 database

Built a v0.1.0 database (schema v1 + a device/class/alarm), then booted v0.2.0 against it:

- The process started and migrated the schema to `user_version` **2** in place.
- The **bootstrap admin was created** on the migrated database (password printed once).
- The pre-existing v0.1.0 data was preserved (`/api/stats` devices = 1).
- `python -m opticorr audit verify` on the upgraded database returned **OK** (chain intact).

```
quickstart: boot=ok  active_alarms=8  situations=1  alarm_count=8
upgrade:    boot=ok  user_version=2  bootstrap_created=ok  devices_preserved=1  audit_verify=OK
```

## Tag

All five gate reports present (`docs/gates/v0.2-phase-0.md` … `v0.2-phase-5.md`). Tagged
`v0.2.0`.

**Gate 5: PASS** — quickstart verified from scratch and upgrade verified from a real
v0.1.0 database.
