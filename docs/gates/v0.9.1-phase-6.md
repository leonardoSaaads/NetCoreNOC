# v0.9.1 — Gate 6: verification and release

Every number below was executed on the release tree.

---

## 1. `make eval` is byte-identical

```
$ python eval/harness.py | sha256sum
c2e8a0ced29d9edf986279d41089ddb68e18da65a46bdc7e9f04811e8b9b6f26  -
```

Identical to v0.9.0, and to v0.9.0's own inheritance of it. The correlation, capture and shadow
paths were not touched, and feedback has never been part of eval.

## 2. Every v0.9.0 test passes, and the `tests/` diff is accounted for

**923 → 960.** No v0.9.0 test was deleted.

```
 tests/test_partial_split.py         | 605 ++++++   NEW — 35 tests
 tests/fixtures/bias-report.txt      | 113 +++--    re-frozen (Gate 4 §3.1)
 tests/fixtures/agreement-report.txt |  58 ++++     re-frozen (Gate 4 §3.2)
 tests/fixtures/shadow-report.txt    |   9 +        re-frozen (Gate 4 §3.3)
 tests/test_rbac.py                  |  58 ++++     NEW — the capability table pinned
 tests/test_agreement.py             |  51 +--      fixture gains a channel dimension
 tests/test_bias.py                  |  46 ++-      fixture gains a partial split + a close label
 tests/test_shadow.py                |  19 ++       NEW — the served_* aliasing assertion
 tests/test_dataset.py               |   4 +-       LabelContext call sites
```

**Every edit to an existing test, and its justification:**

| Edit | Why it was strictly required |
|---|---|
| `test_upgrade.py`: `latest_schema_version() == 9` → `10`; `feedback_exclusion` joins the "no rows seeded" loop | the **only** place in 923 tests coupled to the schema number. The full suite was run with `0010` present and nothing else moved |
| `test_bias.py`, `test_agreement.py`, `test_dataset.py`: `scope=`/`client=` → `label=LabelContext(...)` | DECISIONS #129 — five mechanical call sites; the bundle is what kept `engine.py` inside its 580-line ceiling |
| `test_bias.py`: a fourth situation, a partial split, a close-channel label, three pairs | without them the whole informativeness section prints zeros and **the gate measures nothing about the feature the release exists for** |
| `test_agreement.py`: `PLAN` gains a channel field, four rows read `close` | the per-channel cuts would otherwise be a formality. Chosen to skew toward `confirm` and larger bags, so `organic` reads 50.0 % and `close` 100.0 % |
| `test_bias.py`: `provenance["current"] == 3` → `5` | a count pinned to fixture size; the fixture grew by two labels |
| three re-frozen report fixtures | Gate 4 §3 accounts for **every changed line**, including the hand-checked arithmetic |

Two tests §12 required to pass **unedited**, and did: the v0.7.5 held-card tests
(`tests/test_security_ui.py`) and the F36 boundedness tests (`tests/test_findings.py`,
`tests/test_api.py`).

## 3. Earlier proofs intact

| Proof | Status |
|---|---|
| F34–F45 regression tests | ✅ green |
| the declaration gate | ✅ green; **seeds D1 and D2 both caught** |
| route order and handler hashes | ✅ `score_link` `3bbec323…`, `features` `b7742649…` unchanged |
| `Store` method discipline | ✅ no `Store` method takes `store.lock`; the MRO walk covers the new arrangement |
| one connection and one lock | ✅ green |
| the layer test | ✅ green |
| module-size guard | ✅ nothing over 400; `learn.py` **400**, `shadow_report.py` **400**, `agreement.py` 386, `store/dataset.py` 346, `store/feedback.py` 167 |
| `COHESION_EXEMPT` | ✅ one entry, `engine.py`, ceiling **580**, actual **569** |
| `DEBT_ALLOWLIST` | ✅ empty |
| the documentation guard | ✅ green |
| the pre-registration hash guard | ✅ `bb5bff851588837aa07f21c54b5301f7ada5fec3f8017a5ca4e9d7f7da2cbaef` |
| d3 checksum | ✅ `f2094bbf…` |
| `mypy --strict` | ✅ `Success: no issues found in 142 source files` |
| `ruff check` / `ruff format --check` | ✅ clean |

## 4. Upgrade from a real v0.9.0 database

`tests/test_upgrade.py::test_v091_upgrade_applies_0010_and_leaves_existing_labels_plain`, against a
genuine schema-**9** database (fixture frozen by migration *number*, so a future `0011` cannot
un-freeze it), driven through `fiber_cut.json` with two labels and an audit event:

* `0010` applies; `user_version` 9 → **10**;
* `feedback` and `feedback_member` compared **row for row, value for value** — identical;
* `dataset_stats()` identical, asserted **before the engine starts** so a new `capture_run` cannot
  mask it;
* `feedback_exclusion` **empty**;
* `excluded_count`, `excluded_truncated`, `remainder_together` **NULL on every pre-existing row** —
  existing labels read as exactly what they are, **plain splits and confirms**;
* the audit chain verifies to the **same final hash** — the migration wrote no event;
* first boot after the upgrade writes no exclusion row.

## 5. Packaging, linters, coverage

```
$ python -m build
Successfully built netcorenoc-0.9.1.tar.gz and netcorenoc-0.9.1-py3-none-any.whl
0010_partial_split.sql present in both artifacts

$ python tools/release_check.py
release-check OK: all sources agree on version 0.9.1
```

`pyproject.toml`, `src/netcorenoc/__init__.py` and the top `CHANGELOG.md` heading all read **0.9.1**.
Runtime dependencies: **five**, unchanged.

## 6. The review, the documents, and the tag

| Document | What it is |
|---|---|
| [`../security/SECURITY-REVIEW-0.9.1.md`](../security/SECURITY-REVIEW-0.9.1.md) | Continues from F45; **no finding issued**, and §§1–4 are the checks that back that rather than an omission. §5 records the one contract narrowing found inside the release, measured request-shape by request-shape. §6 is four critical notes |
| [`../architecture/HONEST-JUDGE-0.10-DRAFT.md`](../architecture/HONEST-JUDGE-0.10-DRAFT.md) | **Appended, never rewritten** — a dated §7 recording what v0.10.0 now inherits, and where it contradicts an earlier section it says so |
| [`v0.9.1-test-audit.md`](v0.9.1-test-audit.md) | 31 seeds, 19 caught, 12 missed, the four questions, and "a sample, not a proof" before the results |
| [`../releases/BUILD-REPORT-0.9.1.md`](../releases/BUILD-REPORT-0.9.1.md) | Leads with the asserted-negative count, the channel split and the audit's headline |
| `CHANGELOG.md`, `MIGRATION.md` | `0010`'s contents, that existing labels are untouched, that a partial split changes `penalize` **only when one is given**, and the one narrowed request shape |

## 7. Gate 6 — verdict

| Requirement | Status |
|---|---|
| `make eval` byte-identical | ✅ §1 |
| every v0.9.0 test passes; the `tests/` diff justified | ✅ §2 |
| earlier proofs intact | ✅ §3 |
| upgrade from a real v0.9.0 database | ✅ §4 |
| packaging, linters, guards | ✅ §5 |
| the review from F46 | ✅ no finding issued; the next is still F46 |
| the v0.10.0 specification updated in place, dated | ✅ §6 |
| `CHANGELOG`, `MIGRATION`, build report | ✅ §6 |
| version bump and tag | ✅ `release-check OK`; tagged `v0.9.1` |
| **exactly two intentional behaviour changes** | ✅ the endpoint accepts an exclusion; `penalize` uses it |
| **exactly one migration** | ✅ `0010` |
| **zero new dependencies, capabilities, audit actions, routes, served paths** | ✅ |
| the v0.9.0 pre-registration unedited | ✅ hash green, and **no floor moved** |

## 8. Final state

```
960 tests passed
mypy            Success: no issues found in 142 source files
ruff check      All checks passed!
ruff format     all files already formatted
eval            c2e8a0ced29d9edf986279d41089ddb68e18da65a46bdc7e9f04811e8b9b6f26
release-check   OK: all sources agree on version 0.9.1
```
