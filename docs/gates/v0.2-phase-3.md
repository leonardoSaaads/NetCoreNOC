# v0.2 Gate 3 — Implementation (P0)

Date: 2026-07-20. Verdict: **PASS**.

Full integration in a clean environment; `make qa` green; all v0.1.0 scenario tests still
pass. The nine implementation steps were done in the mandated order (S1 first).

## Step evidence

| Step | Delivered | Where |
|---|---|---|
| **S1** (P0) | F1 XSS fix: `esc()`/`textContent`/`createElement` throughout; split into `index.html` + `app.js` + `style.css` (no inline script/style); d3 v7.9.0 vendored locally; CSP + security headers middleware; `localStorage` token removed | `opticorr/ui/*`, `api.py`, `tests/test_security_ui.py` |
| **S2** (P0) | scrypt passwords (n=2**17), NIST policy, timing-equal dummy verify, per-user/IP exponential lockout, server-side sessions (SHA-256 ids, 30 m idle / 12 h absolute), bootstrap admin, forced change | `auth.py`, `tests/test_auth.py` |
| **S3** (P0) | `rbac.py` single-source permission map; `security` dependency on every `/api` route; 401/403/404 semantics; deny-by-default | `rbac.py`, `api.py`, `tests/test_rbac.py` |
| **S4** (P0) | Chained `audit_log`, append-only triggers, full catalog instrumented, `audit verify`/`export` | `audit.py`, `__main__.py`, `tests/test_audit.py` |
| **S5** (P1) | Service tokens (SHA-256, per-identity, revocable); legacy `OPTICORR_API_TOKEN` → synthetic admin `legacy-token`, deprecation warning, audited once | `auth.py`, `api.py`, `main.py`, `tests/test_findings.py` |
| **S6** (P0) | F3 log-redaction filter + secret-leak scan; F4 community HMAC tag (in-receiver) + quarantine sanitization | `logsetup.py`, `receiver.py`, `events.py`, `store.py`, `tests/test_findings.py` |
| **S7** (P1) | F5 optional TLS + auto-`Secure` cookie; F6 operator warning banners | `main.py`, `runtime.py`, `api.py` |
| **S8** (P1) | Login page, role-aware nav (viewers see no mutating controls), timeline, SSE at `/api/events` with polling fallback, admin screens (users, tokens, config, quarantine, audit) | `opticorr/ui/*` |
| **S9** (P2) | Config via UI (allowlist, retention) with audited changes; env-default / meta-override precedence | `api.py`, `runtime.py`, `main.py` |

## `make qa`

```
ruff check + ruff format --check : clean
mypy --strict                    : no issues in 39 source files
pytest                           : 171 passed
coverage on opticorr/            : 94.63% (gate >= 85%; >= v0.1.0 97.22% - 3)
bandit -c pyproject.toml         : 0 issues
pip-audit                        : no known vulnerabilities
```

All 107 v0.1.0 tests pass unmodified except where behaviour legitimately changed
(the API now requires a role; the legacy-token compatibility path keeps them green —
DECISIONS v0.2 #17). The v0.1.0 scenario suite (`tests/test_scenarios.py`: fiber cut ⇒ 1
situation, OLT storm ⇒ 1 situation, background noise isolated) is unchanged and green.

## Live UI verification (real browser, headless Chromium)

A subprocess server booted with a temp DB; Playwright drove the full bootstrap login and
inspected the running page:

- Bootstrap banner printed the admin password once to stdout; login required the forced
  password change, then granted access.
- The app shell rendered: role tag `ADMIN`, all seven role-appropriate tabs (Situations,
  Timeline, Users, Tokens, Config, Quarantine, Audit), live SSE status.
- **F6**: the "Trap allowlist is empty" banner is shown to the admin (screenshot).
- **F1 in a real browser**: a hostile device label `<img onerror=…><script>…</script>`
  set through the API did **not** execute — `window.__xss` stayed unset and no dialog
  fired, under the live CSP with vendored d3.

Screenshots: `docs/gates/v0.2-phase-3-login.png`, `docs/gates/v0.2-phase-3-ui.png`.

## Ingestion sanity (prime directive 2)

60,000 events through the engine with authenticated, audited API traffic hammering the
store lock concurrently: **0 dropped**, engine drained at ~2,100 traps/s in-process, API
p95 514 ms during the storm, audit chain intact afterward. The trap path acquired no new
lock or I/O (the one datagram-path change is a single in-memory HMAC for the community
tag, DECISIONS v0.2 #16).

**Gate 3: PASS** — ready for Phase 4 (verification).
