# v0.10.0 — Gate 6: the demonstrated guards

The evidence is [`v0.10.0-guard-demonstrations.md`](v0.10.0-guard-demonstrations.md). This document
records only whether the gate's conditions are met.

| Requirement | State |
|---|---|
| every guard has a recorded **red** | **met** — 17 mandatory injections, each with verbatim output |
| every guard has a recorded **green** | **met** — the tree was verified green after every revert |
| every injection has a **named control** that passed under it | **met** |
| the mandatory list complete | **met** — all 17, including F48's two re-run against this tree |
| the ledger lists **every survivor with a disposition** | **met** — 3 survivors, named in Part 2 |
| the limitations section written | **met** — Part 4 |

## The survivors, because the gate is about them

| # | survivor | disposition |
|---|---|---|
| **M3** | the champion measured by a second code path | **a real defect, fixed.** A bag with no partition scored 1.0 — every assertion "respected" |
| **M6** | a floor printed without its detection threshold | **a weak guard, rewritten** to read the rendered output rather than the source's prose |
| **M7** | the CV fold assignment made incident-blind | **a bad injection**, recorded as a known limitation: the property rests on a signature, so the guard cannot be watched to fail |

**M3 is the most valuable result in this release's testing.** The mutation ledger found a defect the
entire rest of the suite was blind to, in the metric this release exists to add — and it was in the
shipped code, not in the mutant.

**Two further defects came from the dead-code gate**, and a third from a probe with a control (F49).
None of the five was found by reading the code, and **none was answered with an allowlist entry**:
`DEBT_ALLOWLIST` is empty and `vulture_allowlist.py` did not grow.
