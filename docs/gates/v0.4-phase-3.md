# Gate v0.4 — Phase 3 (Implementation)

All Phase-3 steps landed in strict order; each ended with `make qa` (and `make eval` where a
scored path could move). Security steps S1–S7 (P0) preceded the P1 UI/breadth work.

| Step | Content | Commit | Status |
|---|---|---|---|
| S1 | Dead code removal + RBAC single source (F8, F9) + dead-code gate | `343ba5d` | done |
| S2 | Least-privilege response shaping (F7) | `ea58371` | done |
| S3 | Reliability: supervision (F10), integrity/fault (F11), `/readyz`, graceful drain | `8c515e5` | done |
| S4/S5 | Supply chain: ship full UI (F12), d3 checksum (F13), container hardening | `adbc523` | done |
| S6/S7 | Trap simulator DSL, security-event correlation (C.3), abuse suite (C.4, F14) | `bbea92e` | done |
| S8 | Network-fault breadth — engine-driven scenario tests (DECISIONS #37) | `bbea92e` | done |
| S9 | Role-gated DOM (A.4) + design-token UI refresh | `c6f1466` | done |
| S10 | `device_id` cutover — **re-deferred to v0.5.0** (DECISIONS #35), ROADMAP line only | — | deferred |

## Gate results

- **`make qa` green**: `ruff check`/`ruff format --check` clean, `mypy --strict` clean, the
  dead-code gate (`vulture` + committed allowlist) clean, `make eval` no gated regression.
- **All v0.3.0 tests pass (renamed)** plus the new v0.4.0 suites: **282 passed** (from 224 at the
  start; the additions are the rename, response-shaping, reliability, supply-chain, DSL/scenario,
  abuse, and UI-role-gating tests). No existing assertion was weakened.
- **`make eval` shows no regression** beyond tolerance: `pairwise_f1=1.0000`, `ari=0.9999`,
  `entity_accuracy=0.4480`, `root_top1=1.0000` — the frozen `v0.2.0` baseline is untouched.
- **Every finding has a passing regression test**:

| Finding | Test |
|---|---|
| F7 response over-disclosure | `tests/test_shaping.py` (unit + role×endpoint + SSE) |
| F8 RBAC audited-denied single source | `test_rbac.py::test_f8_audited_denied_single_source` |
| F9 `config.read` least-privilege | `test_rbac.py::test_f9_config_read_is_its_own_least_privilege_capability` |
| F10 task supervision | `test_reliability.py::test_supervisor_restarts_crashed_task_and_warns` |
| F11 sqlite fault + integrity | `test_reliability.py::test_engine_survives_store_operational_error_…`, `…integrity_check_flags_foreign_key_orphans` |
| F12 UI not shipped in wheel | `test_supply_chain.py::test_all_ui_assets_are_covered_by_package_data_globs` |
| F13 d3 checksum pin | `test_supply_chain.py::test_vendored_assets_match_pinned_checksums` |
| F14 CSRF had no regression test | `test_abuse.py` CSRF cases |

**Gate 3: PASSED.**
