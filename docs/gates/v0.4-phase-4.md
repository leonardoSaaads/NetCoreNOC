# Gate v0.4 — Phase 4 (Verification)

Every item verified; numbers recorded here.

## 1. Rename integrity
- No `opticorr` / `NewProjectNetworj` string survives outside historical records (CHANGELOG,
  `docs/gates/*`, old BUILD-REPORTs/SCOPE/SECURITY-REVIEW-0.2) and the intended legacy-alias
  narrative (MIGRATION, README note, DECISIONS #34, the historical `OPTICORR_API_TOKEN` fact) —
  confirmed by a repo-wide scan.
- **Legacy `OPTICORR_*` env aliases work and warn once**: boot smoke test with
  `OPTICORR_TRAP_PORT` logged `"environment variable OPTICORR_TRAP_PORT is deprecated … use
  NETCORENOC_TRAP_PORT"` exactly once; regression-tested (`test_main.py::test_settings_legacy_env_alias_still_works`).
- **Boots and serves under the new names**: `/healthz` → `{"status":"ok","version":"0.4.0"}`,
  `/readyz` → 200, `<title>NetCoreNOC</title>`, bootstrap banner "NetCoreNOC bootstrap admin",
  `python -m netcorenoc audit verify` → chain OK.

## 2. Security matrix
- `test_rbac.py`: route × role × method over **every** `ROUTE_PERMISSIONS` entry (anonymous +
  viewer/editor/admin); any registered `/api` route absent from the map fails CI (fail-closed);
  403 proven to precede 404 (no existence oracle); denied sensitive reads audited.

## 3. Response shaping
- `test_shaping.py`: role × endpoint field visibility — viewers get coarsened IPs (/24, /48),
  editors/admins get full; `source_ip` dropped below admin, `community_tag` below editor; the SSE
  live path is shaped identically (direct-ASGI test). No over-disclosure to viewers.

## 4. Auth / session / CSRF / XSS
- Auth throttle/lockout/no-enumeration, session fixation/expiry/revocation, cookie flags
  (`test_auth.py`); CSRF (missing/renamed header, origin/host mismatch → 403; valid → 200; Bearer
  exempt) and `SameSite=Strict`/`HttpOnly` (`test_abuse.py`); hostile strings through the real
  ingest render as inert JSON on the shaped viewer path (`test_abuse.py`, `test_security_ui.py`).
- **Secret-leak scan clean** over the login/token flows (`test_findings.py`), and the boot smoke
  log showed no secret value.

## 5. Reliability
- Task-kill recovery + backoff + warning; DB-locked/operational-error handled without crash or
  chain break; startup integrity/FK check warns (never crashes); graceful drain leaves the chain
  intact; `/readyz` 503 on saturation with no detail leak (`test_reliability.py`, 9 tests).

## 6. Corpus / eval
- `make eval` deterministic; **no gated regression** — `pairwise_f1=1.0000`, `ari=0.9999`,
  `entity_accuracy=0.4480`, `root_top1=1.0000` vs the frozen `v0.2.0` baseline. New security/fault
  scenarios are engine-driven tests (DECISIONS #37): login-burst → one situation, chassis
  containment → one situation, cross-device BGP stays separate cold (`test_security_scenarios.py`).

## 7. UI
- Role-gated DOM: admin panels admin-gated and pruned from a non-admin DOM; mutating controls
  behind `canEdit()`/`isAdmin()`; UI stays four files; CSP unchanged and forbids inline
  (`test_security_ui.py`). d3 checksum asserted (`test_supply_chain.py`).

## 8. Migration
- Populated DB → reopen → learned affinity + situations + audit chain survive; no migration runs
  (`user_version` stays at latest); healthy integrity (`test_upgrade.py`). Bootstrap unaffected.

## 9. Standards / tooling
- `docs/SECURITY-REVIEW-0.4.md` compliance table finalised (met / partial / N-A, each with proof).
- **`bandit` clean**, **`pip-audit` — no known vulnerabilities**, **dead-code scan clean**
  (`vulture` + allowlist).
- **Coverage 95.02%** (`--cov=netcorenoc`), ≥ 85% and not below the v0.3.0 figure minus 3 points.
- **Tests: 283 passed** (224 at start + the v0.4.0 suites).

**Gate 4: PASSED.**
