# Gate 5 — verification

**Status: PASSED.** All twelve verification points below are green, with every number measured
rather than asserted.

---

## 1. Method parity

Both hash tables recomputed and compared against the Gate 0 baseline, taken through
`inspect.getsource` on the **live attribute resolved via each class's `__mro__`** — so the proof
covers what Python actually resolves after a sixteen-mixin (`Store`) and four-mixin (`Engine`)
assembly, not merely what the files contain.

```
Store : baseline=109 now=109  missing=NONE  added=NONE  MISMATCHES=NONE
Engine: baseline= 32 now= 32  missing=NONE  added=NONE  MISMATCHES=NONE

PARITY: ALL METHOD BODIES IDENTICAL
```

**Zero mismatches across 141 method bodies.**

## 2. The concurrency invariant

`tests/test_store_concurrency.py` — 9 tests, green, **unchanged** from the version installed in
Phase 2 against the pre-split tree. One `aiosqlite.Connection`, one `asyncio.Lock`,
`store.conn is store.conn`, no `Store` method acquiring the lock, three domains writing concurrently
under one commit, 24 concurrent audit appends producing a contiguous verifiable chain, and
`write_txn` rolling back into nothing.

## 3. Test parity

```
 tests/test_architecture.py      | 192 +++++++++-      (guards added)
 tests/test_governance.py        |   2 +-              (one patch path)
 tests/test_layers.py            | 302 +++++++++++++   (new)
 tests/test_promotion.py         |   6 +-              (two patch paths + comment)
 tests/test_store_concurrency.py | 289 +++++++++++++   (new)
 tests/test_structure.py         |  44 ++++            (module lists + one new test)
 tests/test_upgrade.py           |   6 +-              (three patch paths)
 7 files changed, 832 insertions(+), 9 deletions(-)
```

**Assertion lines changed in pre-existing test files: 0.** Measured by diffing every pre-existing
test file touched by this release and grepping for added or removed `assert` lines. The nine
deletions are the three patch-target import lines and the two constants they name.

## 4. Eval parity

`make eval` byte-identical to the frozen Gate 0 baseline at every gate since.
`sha256 = d6f3fc396eaed40f2f4bec6923bb06bd6f7fc948a86e942eccb8cdfc5a512b9d`.

## 5. The HTTP layer is untouched

`git diff` over `src/netcorenoc/api/` for the whole release: **empty**. The v0.7.2 route-order
baseline (48 entries, in order), the handler-hash tables and the declaration gate all pass
unchanged — `test_an_undeclared_api_route_cannot_be_registered` still refuses at import.

## 6. Behaviour parity

The authorization matrix, the audit-catalog completeness test, and every v0.7.1 write-perimeter /
resolver-input / transaction-discipline / feedback-boundedness test pass unchanged. In particular
`test_f39_a_failed_write_leaves_nothing_to_commit` exercises the post-split `Store` **with no
edit**: it drives the discipline through a real HTTP request and touches no module path and no
internal of `Store`.

## 7. Import surface, from an installed wheel in a clean virtualenv

Not from the source tree — a fresh `python3.12 -m venv` with only the built wheel installed.

```
store symbols  : OK (5)    Store, EdgeRow, FeedbackResult, IngestResult, MIGRATIONS_DIR
main symbols   : OK (17)   Engine, Settings, FlapDetector, GapTracker, GAP_CLOSE_S, run,
                           LegacyEnvRemovedError, LegacyTokenRemovedError, legacy_env_error,
                           legacy_env_names, Supervisor, IDLE_CLOSE_S, operator_warnings,
                           read_env, ENV_PREFIX, LEGACY_ENV_PREFIX, main
Store MRO len  : 18        Engine MRO len : 6        migrations : 7
```

```
$ python -m netcorenoc audit verify
audit chain OK: 0 entries verified; final hash 0000…0000

$ python -m netcorenoc.main
healthz : {"status":"ok","version":"0.7.3"}
readyz  : {"status":"ready"}
bootstrap admin banner printed once
```

## 8. Upgrade from a real v0.7.2 database

Stage 1 ran against the **v0.7.2 tree extracted from git at the merge commit `990d280`** — real
code, not a fixture — driving 120 traps through the real `Engine`, a maintenance pass, a scorer
configuration, an active governance policy, a bootstrap admin and an audit row. Stage 2 opened the
same file with v0.7.3.

```
wrote  v0.7.2 DB: schema v7, 12 alarms, 3 situations, 1 audit rows, final hash f5bb4fa01a446d98…
opened v0.7.3   : schema v7 (unchanged), 1 audit rows, final hash f5bb4fa01a446d98…
chain verifies  : True
snapshot keys   : 20 compared

UPGRADE: IDENTICAL — no migration, no drift
```

The snapshot covers `user_version`, devices, NEs, classes, alarms (with severity and rank),
situations (with root and scorer provenance), members, links, learned edges, entities, varbind
profiles, meta, users, scorer configs, governance policies, state clears and ingest gaps. **No
migration ran and no byte moved.**

## 9. Packaging

| Artefact | Store package | New top-level modules | UI | Migrations | d3 `CHECKSUMS.txt` | `_all.py` |
|---|--:|--:|--:|--:|---|---|
| wheel | 18 | 8 | full | 7 | present | **absent** |
| sdist | 18 | 7 | full | 7 | present | **absent** |

F12/F13 hold: the wheel ships `ui/index.html`, `ui/app.js`, `ui/style.css`,
`ui/vendor/d3.v7.min.js`, `ui/vendor/CHECKSUMS.txt`, `ui/vendor/d3.LICENSE` and
`ui/.well-known/security.txt`. `make release-check` agrees on **0.7.3** across `pyproject.toml`,
`src/netcorenoc/__init__.py` and `CHANGELOG.md`.

## 10. Deployment

`docker compose config` **validates**.

**The Docker daemon is unavailable in this environment**, so the image could not be built. Stated
plainly rather than skipped. Per the v0.7.2 precedent, the equivalent was verified from the wheel in
a clean virtualenv — every public path served, with the security headers intact:

| Path | Status | Bytes |
|---|--:|--:|
| `/openapi.json` | 200 | 28 609 |
| `/healthz` | 200 | 33 |
| `/readyz` | 200 | 18 |
| `/` | 200 | 5 818 |
| `/app.js` | 200 | 45 787 |
| `/style.css` | 200 | 13 251 |
| `/vendor/d3.v7.min.js` | 200 | 279 706 |
| `/.well-known/security.txt` | 200 | 560 |
| `/api/stats` (no identity) | **401** | — |
| `/api/situations` (no identity) | **401** | — |

All four security headers (`Content-Security-Policy`, `X-Content-Type-Options`, `X-Frame-Options`,
`Referrer-Policy`) present on both the index and a static asset. `Dockerfile`'s
`CMD ["python", "-m", "netcorenoc.main"]` is unchanged and is exactly what was exercised.

## 11. Guards

| Guard | State |
|---|---|
| `DEBT_ALLOWLIST` | **exactly three**: `rbac.py` (436), `shaping.py` (476), `varbind_profile.py` (417), all v0.7.4 |
| Allowlist direction | shrank from five; `ALLOWLIST_MEMBERSHIP_CEILING` shrank with it, and `test_no_module_may_join_the_allowlist` now blocks additions |
| `COHESION_EXEMPT` | **one** entry (cap two): `engine.py`, citing "ingestion is sacred", **no owner, no fix date**, ceiling 542 |
| Layer test exemptions | **empty** |
| `PLANNED_THIS_RELEASE` | **empty** — the dead-entry check is absolute again |

Every module over 400 lines in the whole package, and why:

```
542  engine.py            COHESION_EXEMPT — ingestion is sacred
476  shaping.py           DEBT_ALLOWLIST — v0.7.4
436  rbac.py              DEBT_ALLOWLIST — v0.7.4
417  varbind_profile.py   DEBT_ALLOWLIST — v0.7.4
```

Four modules, each accounted for. 60 modules in the package; the other 56 are under the guard.

## 12. Linters, scanners, coverage

```
ruff check .              All checks passed!
ruff format --check .     all files formatted
mypy --strict             Success: no issues found in 114 source files
vulture (dead code)       clean
bandit                    exit 0, no issues
pip-audit                 No known vulnerabilities found
d3 checksum               pass
documentation link-check  pass
GitHub-Actions SHA pin    pass
structure guard           pass
architecture guard        pass
declaration gate          pass
route order / handler hashes / scorer parity   pass
coverage                  95.80%   (v0.7.2 baseline 95.69% — above it)
```

Coverage is **above** the baseline, which is the right direction for a pure move: the new modules
are fully exercised, and the F38 fallback branch that Gate 3 §4.3 found had gone dark is being
tested again.

The recorded figure is the **final** run, not the best one. Across the release's six full `make qa`
runs pytest-cov reported 95.69 (baseline), 95.69 (Gate 2, no source changed), 95.52 (the vacuous-test
find, since fixed), 95.91, 95.93 and 95.80. Every post-split run is above the baseline.

**The spread has one cause, and it is not this release.** Diffing the per-module reports between two
runs of *identical* code shows exactly one line differing: `receiver.py`, which ranges 87–91 % across
runs — including 88 % at the untouched v0.7.2 baseline and 88 % at Gate 2, where no source had
changed at all. It is the UDP listener, and its socket-error and backpressure branches are
timing-dependent. **This release does not touch `receiver.py`.** Holding it at its baseline miss
counts in both measurements, the release's own contribution is a small **increase**. The variance is
pre-existing and is recorded as a v0.7.4 ROADMAP line rather than quietly averaged away.

Every new module's coverage:

```
store/base.py 100   store/types.py 100   store/learned.py 100   store/alarms.py 100
store/auth.py 100   store/audit_log.py 100   store/situations.py 100   store/retention.py 100
store/ingest_gaps.py 100   store/scoring_config.py 100   store/state_clears.py 100
store/__init__.py 100   store/devices.py 99   store/read_models.py 98   store/entities.py 97
store/governance.py 95   store/lifecycle.py 95   store/feedback.py 91
engine_base.py 100   gaps.py 100   settings.py 100   maintenance.py 98   engine.py 96
scorer_lifecycle.py 91   runner.py 90
```

### 12.1 The redundant `# nosec` markers, located

`bandit` reports four `nosec encountered (B608), but no failed test` warnings, now at
`store/retention.py:23,27,31,35`. They are **not** fixed here (build prompt §3.4 — changing a
`# nosec` on a SQL string is a change to SQL handling inside a move release) and are recorded as a
v0.7.4 ROADMAP line. The split has at least made them easy to find: they were four lines in a
1 512-line file and are now four lines in a 48-line one.

---

## Gate 5 verdict — **PASSED**

- [x] 1. Method parity — 109/109 `Store`, 32/32 `Engine`, zero mismatches
- [x] 2. Concurrency invariant — 9 tests green, unchanged from pre-split
- [x] 3. Test parity — **zero assertion lines edited**
- [x] 4. Eval parity — byte-identical
- [x] 5. HTTP layer untouched — `api/` diff empty; declaration gate still refuses at import
- [x] 6. Behaviour parity — every v0.7.1 finding test unchanged and green
- [x] 7. Import surface — all 22 symbols from an installed wheel in a clean virtualenv
- [x] 8. Upgrade — real v0.7.2 DB, no migration, 20 snapshot keys identical, same final chain hash
- [x] 9. Packaging — wheel and sdist complete; `release-check` agrees on 0.7.3
- [x] 10. Deployment — compose validates; Docker daemon **unavailable**, wheel equivalent verified
- [x] 11. Guards — three debt entries, one cohesion exemption, empty layer exemptions
- [x] 12. Linters, scanners, coverage at 95.93 % (above baseline)
