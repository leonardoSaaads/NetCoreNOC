# v0.3.0 — Phase 5 gate: Release

Phase 5 turns the verified build into a tagged release: version bump, changelog, migration and
upgrade docs, README accuracy, and the build report — with a final green `make qa && make eval`
as the tag precondition.

## Release artifacts

- **Version** — `opticorr/__init__.py` and `pyproject.toml` → `0.3.0`; `/healthz` reports it.
- **CHANGELOG.md** — a `[0.3.0]` section (Added / Changed / Removed / Migration) written to
  Keep-a-Changelog form.
- **MIGRATION.md** — the v0.2.0 → v0.3.0 path updated for both migrations (`user_version` 2 → 4),
  the `OPTICORR_API_TOKEN` removal, and "day 0 is identical to v0.2.0".
- **README.md** — intro now states the entity model; the score section documents entity
  affinity and the varbind profiler; the **cold-start claim is corrected to be honest** — with
  nothing learned, two alarms group only on the same NE and within Δt < 30·ln 2 ≈ 21 s (the
  temporal term alone must clear the 0.5 threshold), not merely "same device"; the config table
  marks `OPTICORR_API_TOKEN` as removed and notes SNMPv1.
- **docs/BUILD-REPORT-0.3.md** — the full build narrative, quality table, and prime-directive
  ledger.

## Final gate (`make qa && make eval`)

- `ruff check` / `ruff format --check` — clean.
- `mypy` (strict) — clean, 50 files.
- `pytest` — **224 passed**, 0 skipped.
- Branch coverage — **94.94%** (gate ≥ 85%).
- `make eval` — no gated regression, learning `entity_accuracy` +0.4158; `make eval --cold` —
  exact parity (`pairwise_f1 = 1.0000`, `ari = 0.9999`).
- `bandit` — 0 issues; `pip-audit` — no OptiCorr runtime-dependency findings.

## Tag

`v0.3.0` is tagged from the commit that carries this report, on branch `claude/session-njpyv4`,
after the final gate came back green.

**Gate 5: PASS.** The release is documented, the schema-migration and upgrade paths are
described, the README no longer overstates cold-start behaviour, and the tag is cut from a green
`make qa && make eval`.
