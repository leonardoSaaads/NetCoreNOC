# Gate 4 — Verification (v0.7.1)

**Status: passed.** All eleven verification items green, every number recorded.

---

## 1. Correlation parity — byte-identical

```
$ .venv/bin/python eval/harness.py > eval-final.txt
$ diff eval-v0.7.0.txt eval-final.txt && echo "EVAL BYTE-IDENTICAL TO v0.7.0"
EVAL BYTE-IDENTICAL TO v0.7.0
```

`sha256` of the harness output, before and after: **`c2e8a0ced29d9edf986279d41089ddb68e18da65a46bdc7e9f04811e8b9b6f26`** — the same bytes.

The engine, window, scorer and ingest path are provably untouched: `receiver.py`, `correlate.py`,
`scoring.py` and `preview.py` have **zero** diff against v0.7.0; the v0.6.0 `test_f24_*` hot-path
source assertions and the v0.7.0 `test_f33_datagram_received_gained_nothing` are green. The only
engine-side change is `Engine.apply_feedback` and its transaction boundary — a feedback path, and
the eval drives no feedback (`grep -rn 'apply_feedback\|add_feedback' eval/ tools/` → nothing).

## 2. Empty-policy parity — three documented changes, no fourth

`test_upgrade.py::test_v071_upgrade_changes_no_behaviour_except_the_three_documented_changes` builds
a genuine v0.7.0 database (migrations frozen at `0006`), replays `fiber_cut.json` through the real
engine, then reopens the same file with `0007` present and asserts each item **explicitly**:

| Property | Assertion |
|---|---|
| Grouping | `partition_of(new) == before_partition` |
| Learned edges | `graph_snapshot(min_edge_n=0)["edges"]` identical |
| Stats | `stats()` identical |
| Forgetting epoch | `(A.epoch, E.epoch)` identical |
| Audit chain | `verify_chain(new).ok` |
| Scorer provenance | active config still present |
| Governance | `active_governance_ids() == {}` — still no policy |
| **Change 3 (F38)** | `list_situations(None, 500)`, `list_situations("open", 500)` and `timeline_marks(1000)` **identical to the v0.7.0 query** — the SQL changed, the unrestricted result set did not |
| `ne_id` containment | every rendered mark's keys are exactly `{ts, device, class, kind}` — the filter column never reaches a response |
| **Change 1 (F37)** | nonexistent target → **404**; real target → **200** |
| **Change 2 (F36)** | the repeat answers 200 with the **same body**, records **1** row not 2, leaves the pair mass unchanged, and does not move the epoch |
| Everything else | `/api/me` reports `{"scoped": false, "ne_count": null}`; `/api/stats`, `/api/graph`, `/api/timeline`, `/api/entities`, `/api/scope` all 200 |

## 3. Write perimeter — generated over `ROUTE_PERMISSIONS`

`test_f34_every_mutating_route_below_admin_resolves_scope` walks the map. Of 19 non-`GET` routes,
5 are below `admin`; 3 are NE-bearing and must resolve scope; 2 (`logout`, `password`) act on the
caller's own session/account and are exempt by an explicit, justified list inside the test.

Verified to **fail on the unmodified v0.7.0 tree** (`git stash push -- src/`):

```
E   AssertionError: routes outside the write perimeter:
E     POST /api/labels (label.write) does not call scope_for()
E     POST /api/situations/{sid}/close (situation.close) does not call scope_for()
E     POST /api/situations/{sid}/feedback (feedback.write) does not call scope_for()
```

Out-of-scope and nonexistent are indistinguishable on every write route: the scope denial routes
into the handler's pre-existing not-found branch, and
`test_f34_scope_is_enforced_on_the_editor_write_routes` asserts
`feedback.json() == missing.json()` for the same status.

## 4. Resolver inputs — no input is writable by a scopable role

`test_f35_no_resolver_input_is_writable_by_a_scopable_role`, also verified failing on v0.7.0:

```
E   AssertionError: list_ne_for_scope() selects operator-writable column(s) ['label']:
E   the scope decision must read NE identity and address only (F35, DECISIONS #66)
```

It asserts the **projection** of `list_ne_for_scope()` carries no operator-writable column, and that
`visible_nes()` returns the identical `Scope` whether hostile labels are present or stripped.
`test_f35_the_scopable_roles_are_exactly_viewer_and_editor` pins the premise.

Both proofs-of-concept now fail to escalate:

| PoC | v0.7.0 | v0.7.1 |
|---|---|---|
| Label glob (`{"editor": ["core-*"]}`, editor labels an out-of-scope device `core-pwned`) | `/api/me` scope `ne_count` 1 → **2**; graph gains the node | scope unchanged; the write is refused **404**; a stored label glob resolves to `ne_count: 0` |
| Label collision (editor copies an in-scope NE's label onto an out-of-scope one) | timeline marks **doubled** | marks unchanged — the filter is on `ne_id` |

## 5. Feedback — idempotent, bounded, epoch-correct, attributed

| Property | Test | Result |
|---|---|---|
| Idempotent per `(situation, verdict)` | `test_f36_repeated_feedback_is_idempotent_and_bounded` | 80 posts (60 confirm + 20 split) → **2** rows |
| Bounded effect | same | epoch **unmoved**; pair mass stays above `0.1 ×` its start (was `1.824e-05`) |
| A changed verdict applies once | `test_f36_a_changed_verdict_still_applies_once` | split penalizes; repeat is a no-op; confirm-after-split corrects |
| Epoch advances only on close | `test_f36_closing_a_situation_still_ticks_the_epoch` | exactly `+1` per closed situation |
| Attributed | `test_f36_feedback_records_its_author` | `principal_ref='token:1'`, `role='admin'` |

## 6. Labels — 404, the same 404, orphans reaped, growth bounded

- Nonexistent target → **404** for device ids 900000–900004 and for a nonexistent class; **0** rows
  persist (`test_f37_a_label_write_to_a_nonexistent_target_is_rejected`).
- Out-of-scope target → the **same** 404 body, from the same handler
  (`test_f34_scope_is_enforced_on_the_editor_write_routes`).
- Real targets still work (`test_f37_label_writes_to_real_targets_still_work`).
- Migration removes existing orphans: **6** rows in the gate fixture, with both real labels intact.
- No unbounded growth path remains: every write is now bounded by the existence of a `device` or
  `alarm_class` row.

## 7. Truncation — `min(limit, own_total)` regardless of out-of-scope volume

`test_f38_truncation_is_applied_after_the_scope_filter`: a scoped viewer at `limit=2` receives the
same count before and after out-of-scope traffic they cannot see (v0.7.0: **2 → 0**), and still
receives their timeline marks at `limit=2` (v0.7.0: **0**).

`test_f38_the_unrestricted_result_set_is_unchanged` asserts the unscoped path is byte-identical to
the direct v0.7.0 store query, for `list_situations(None)`, `list_situations("open")` and
`timeline_marks`.

The `MAX_SCOPE_PARAMS` (30 000) fallback is covered and proven to **agree exactly** with the bound
path (`test_f38_the_large_scope_fallback_agrees_with_the_bound_path`), and an empty scope is
answered without a query (`test_f38_an_empty_scope_reads_nothing_without_a_query`).

## 8. Transactions

- A forced failure after a mutation leaves nothing to commit: `audit.write_event` made to raise
  inside `POST /api/users/{uid}/role`, then a commit from an unrelated caller — the role stays
  `viewer` (`test_f39_a_failed_write_leaves_nothing_to_commit`). On v0.7.0 it became `admin`, with
  no audit row.
- `POST /feedback` commits **exactly once** beyond identity resolution
  (`test_f39_feedback_commits_exactly_once`; v0.7.0: **2**).
- Every mutating handler reaches the helper, asserted over the source
  (`test_f39_every_mutating_handler_uses_the_transaction_helper`), which fails on v0.7.0 with *"the
  transaction helper is gone"*.
- `store.rollback()` in `api.py`: **0 → 1** (in `write_txn`, used by all 22 mutating handlers).

## 9. Migration

Populated v0.7.0 DB → migrate → verified (`test_migrate_populated_v070_database_…`, and the Gate 2
evidence):

| Check | Result |
|---|---|
| `user_version` | 6 → **7** |
| Data intact | `alarm`, `situation`, `situation_alarm`, `edge`, `audit_log`, real `label` rows unchanged |
| De-duplicated `feedback` | **6** removed; survivors are the **earliest** by `created_at` (`confirm@100.0`, `split@400.0`) |
| Orphan `label` rows | **6** removed; both real labels survive |
| `UNIQUE` index live | ✅ `UNIQUE constraint failed: feedback.situation_id, feedback.verdict` |
| Attribution columns | present, nullable, `NULL` for pre-`0007` rows (deliberately not backfilled) |
| Audit chain | verifying, head hash **byte-identical** (`a52f01e32ec80c7b…`) across the migration |
| `PRAGMA foreign_key_check` | clean |
| `PRAGMA integrity_check` | `ok` |
| Seeded rows | **0** governance, **0** of any kind |
| Idempotent | re-open is a no-op |

**Shipped in a freshly built wheel and sdist (F12):**

```
dist/netcorenoc-0.7.1-py3-none-any.whl   → netcorenoc/migrations/0007_write_perimeter.sql  ✅
                                           + all 9 ui/ files including vendor/d3.v7.min.js  ✅
dist/netcorenoc-0.7.1.tar.gz             → src/netcorenoc/migrations/0007_write_perimeter.sql ✅
```

Installed into a clean Python 3.12 venv from the wheel alone:

```
installed version: 0.7.1
schema version from wheel: 7
F36 unique index present: True
governance rows seeded: 0
d3 sha256: f2094bbf6141b359722c4fe454eb6c4b0f0e42cc10cc7af921fc158fceb86539
CHECKSUMS.txt: f2094bbf6141b359722c4fe454eb6c4b0f0e42cc10cc7af921fc158fceb86539  d3.v7.min.js
```

The d3 checksum is intact.

## 10. Documentation

- The "scoping is not tenant isolation" statement is still present in `SCOPE-0.7.md`, `DESIGN.md`,
  `README.md`, `MIGRATION.md` and the UI, and still asserted by
  `test_f32_scoping_is_not_tenant_isolation_is_documented`.
- The **F32 correction is present and points to F38** (and to F34):
  `SECURITY-REVIEW-0.7.md` §2 now carries a dated *"Correction to F32 — added 2026-07-29 (v0.7.1)"*
  block, the F32 row's status reads **partially superseded**, and the two §5 mapping rows name their
  corrections. The published claim is left intact, not rewritten.
- Two statements that had become wrong were fixed: `README.md` and the UI's Governance hint both
  advertised a "name glob" selector.

## 11. Toolchain

| Gate | Result |
|---|---|
| `ruff check .` | ✅ All checks passed |
| `ruff format --check .` | ✅ 151 files already formatted |
| `mypy` (strict) | ✅ no issues in **70** source files |
| `bandit` | ✅ no findings |
| `pip-audit` | ✅ **No known vulnerabilities found** |
| dead-code (`vulture`) | ✅ clean, no allowlist entries added |
| documentation link check (`test_structure.py`) | ✅ 25 passed |
| structure guard | ✅ |
| GitHub-Actions SHA-pin lint (`test_workflows.py`) | ✅ |
| d3 checksum (`test_supply_chain.py`) | ✅ |
| scorer parity (`test_correlate.py`) | ✅ |
| generated authorization matrix (`test_rbac.py`) | ✅ |
| audit catalog completeness (`test_audit.py`) | ✅ |
| `docker compose config` | ✅ |
| **Coverage** | **95.46 %** vs v0.7.0's **95.43 %** — **+0.03 pt**, raised not lowered (floor 85 %) |
| **Tests** | **524 passed** (499 → 524) |

## 12. Invariants held

| Quantity | v0.7.0 | v0.7.1 |
|---|---|---|
| Runtime dependencies | 5 | **5** |
| `PERMISSIONS` | 28 | **28** |
| `ROUTE_PERMISSIONS` | 39 | **39** |
| `PUBLIC_ROUTES` | 1 | **1** |
| `AUDITED_DENIED_PERMISSIONS` | 14 | **14** |
| `audit.ACTIONS` | 30 | **30** |
| New modules / moved files | — | **none** |

**Gate 4: passed.**
