# Gate v0.4 — Phase 5 (Release)

## Release artifacts
- `CHANGELOG.md` — v0.4.0 entry (rename + hardening + corpus, honest caveats).
- `MIGRATION.md` — v0.3.0 → v0.4.0 rebrand section (env aliases, cookie/header, one re-login,
  no-migration upgrade path).
- `README.md` — NetCoreNOC identity, corrected repo URL and env table, rebrand note.
- `SECURITY.md` — operator guidance incl. the CIS-style container-hardening + backup section.
- `docs/SECURITY-REVIEW-0.4.md` — F7…F14 finding→fix→test map + compliance table.
- `docs/BUILD-REPORT-0.4.md` — what changed, quality numbers, findings, reliability, corpus
  inventory, decisions #34…#38, ordered deferred items, honest caveats.
- Version bumped to **0.4.0** (`pyproject.toml`, `netcorenoc/__init__.py`).

## Quickstart verified from scratch (new names)
Fresh DB, high ports: `python -m netcorenoc.main` →
- `/healthz` = `{"status":"ok","version":"0.4.0"}`, `/readyz` = 200, `<title>NetCoreNOC</title>`.
- Bootstrap banner "NetCoreNOC bootstrap admin created" printed once.
- `python -m netcorenoc audit verify` → chain OK.

## Upgrade verified from a real v0.3.0 database with learned state
`test_upgrade.py`: a populated DB (learned device-affinity edge + a grouped situation + an audit
row) is reopened with fresh objects — **no migration runs** (`user_version` stays at the latest),
the learned affinity matrix and situations survive, `engine.learner.device_affinity(...)` reloads
> 0, the audit chain still verifies, and integrity is clean. Legacy `OPTICORR_*` env aliases warn
once and work (boot smoke test + `test_main.py`); the cookie rename forces exactly one re-login.

## Final gate
- `make qa` green: ruff / ruff format / mypy --strict / dead-code / eval — all clean, no gated
  regression.
- **283 tests pass**, **coverage 95.02 %**, `bandit` clean, `pip-audit` no known vulnerabilities.
- Tag **`v0.4.0`**.

**Gate 5: PASSED.**
