# Gate 5 — v0.7.4 verification

**Status: PASSED.** All twelve verification points green, every number recorded.

```
754 passed      coverage 95.81 %  (v0.7.3: 95.80 %)      make eval BYTE-IDENTICAL
ruff clean      mypy --strict clean (121 files)          vulture clean
bandit clean    pip-audit: no known vulnerabilities      release-check: 0.7.4
```

---

## 1. Function parity — 56/56

Phase 0's hash table recomputed over the split tree:

```
  shaping.py             21/21
  rbac.py                 7/7
  varbind_profile.py     28/28

PHASE 5 RECOMPUTATION: 56/56 functions hash identically to Phase 0
```

Every function and method moved as **identical text**, decorator lines included. Matching is by
hash rather than by name, which is the stronger check: it would catch a body moved into the wrong
module as well as a body edited.

`rbac.py`'s eight tables and three asserts are data and prose, not functions. Their parity is proved
by the identity assertions instead — stronger, because they hold at runtime rather than over text.

Neither transitional `_all.py` survives.

---

## 2. Test parity

```
$ git diff --stat 8b2c175 -- tests/
 tests/test_architecture.py  |  26 +--
 tests/test_declaration.py   | 179 ++++++++++++++++-
 tests/test_documentation.py | 457 +++++++++++++++++++++++++++++++++++++++++++
 tests/test_governance.py    |   2 +-
 tests/test_layers.py        |   1 +
 tests/test_rbac.py          |  77 ++++++++
 tests/test_structure.py     |  11 ++
 7 files changed, 734 insertions(+), 19 deletions(-)
```

**Nineteen deleted lines in total.** Every one is listed below with its justification; there is no
twentieth.

| # | File | Removed | Category |
|--:|---|---|---|
| 1–12 | `test_architecture.py` | the whole `DEBT_ALLOWLIST` body — three entries and their comment block | **the release's purpose**: the allowlist reaches zero |
| 13–16 | `test_architecture.py` | `ALLOWLIST_MEMBERSHIP_CEILING`'s three names and one comment line | the ceiling shrinks with it, so *any* new entry now fails |
| 17 | `test_architecture.py:118` | `assert "rbac.py" in modules` | guard-the-guard canary; `rbac.py` no longer exists, so it now names `rbac/tables.py` |
| 18 | `test_declaration.py:28` | `from netcorenoc.api.declare import DeclaredRoutes, UndeclaredRouteError` | import line, extended for the F40/F41 tests |
| 19a | `test_declaration.py:179` | `source = Path(rbac.__file__).read_text(...)` | **moved source path** → `rbac.tables.__file__` |
| 19b | `test_governance.py:539` | `"src/netcorenoc/shaping.py"` | **moved source path** → `"src/netcorenoc/shaping/__init__.py"` |

**No test assertion was edited.** The two moved paths are the files a test *reads*; the assertions
over what it finds there are byte-identical. Both were predicted in Phase 0 §3.3 rather than
discovered mid-split — and both are the failure mode `tests/apisource.py` exists for (DECISIONS #84),
where a source-scanning guard silently begins scanning almost nothing.

The insertions are: the new documentation guard (457), the F40/F41 regression tests (179), the two
authorization single-sourcing tests (77), and module-list additions to `SUBMODULES` (11) and
`LAYER_OF` (1).

### §4 required no test edits at all

Build-prompt §9.2 permits assertion edits "where §4's two changes require it". **None were
required.** The route-order table, the authorization matrix and both route-map completeness tests
pass unedited (§5 below), because the gate became stronger without becoming narrower.

---

## 3. Eval parity

```
$ diff eval-baseline-v0.7.3.txt eval-phase5.txt
$ echo $?
0
```

**Byte-identical**, `sha256 c333ca46a0f7741c727ebccae86828cfcf417b9303f87aa9874567b421753132`.
Every gated metric `+0.0000`: `pairwise_f1`, `ari`, `entity_accuracy`.

---

## 4. Earlier proofs intact

| Proof | From | State |
|---|---|---|
| Route-order table (48 entries, in order) | v0.7.2 | `test_route_table_order_is_unchanged` **passes, unedited** |
| No duplicate `(method, path)` | v0.7.2 | passes |
| Handler-hash table (40 handlers) | v0.7.2 | `tests/test_findings.py` — **11 passed** |
| `Store` method hashes | v0.7.3 | `tests/test_store.py` — **13 passed** |
| Store concurrency: one `Store`, one connection, one `store.lock` | v0.7.3 | `tests/test_store_concurrency.py` — **9 passed** |
| Layer rule with an **empty** exemption list | v0.7.3 | `tests/test_layers.py` — **13 passed** |
| d3 SHA-256 pin, wheel/sdist shipping (F12/F13) | v0.5.0 | `tests/test_supply_chain.py` — **4 passed** |
| GitHub-Actions SHA-pin lint | v0.5.0 | `tests/test_workflows.py` — **7 passed** |
| F35 resolver-input invariant | v0.7.1 | **passes, unedited** |
| F1–F39 regression suite | v0.1–v0.7.1 | all green |

`tests/test_layers.py` needed **one** line — `varbind_accum: engine`. The two new *packages* needed
none: `_module_files()` keys a packaged module by its package name, so `rbac/policy.py` and
`shaping/scope.py` are classified under `rbac` and `shaping`, which the table already carried.

---

## 5. The gate

| Property | Check | Result |
|---|---|---|
| Undeclared route via **decorator** fails | `test_an_undeclared_api_route_cannot_be_registered` | pass |
| Undeclared route via **direct registration** fails `create_app` | `test_f40_a_route_registered_without_the_decorator_fails_create_app` | pass |
| Undeclared route via an **unwrapped verb** fails `create_app` | `…_by_an_unwrapped_verb_…[put]`, `[patch]` | pass |
| The assertion runs **before `create_app` returns** | `test_f40_the_assertion_runs_before_create_app_returns` (parses the source) | pass |
| Non-`/api` path outside the allowlist is refused | `test_f41_a_non_api_path_outside_the_allowlist_is_refused` ×4 | pass |
| **Every served public path still accepted** | `test_f41_every_currently_served_public_path_is_still_accepted` ×8 | pass — **and passes on v0.7.3's source too**, which is what proves no narrowing |
| Allowlist matches what `routes_static.py` registers | `test_f41_the_allowlist_matches_what_is_actually_served` | pass |
| Allowlist matches the **live** non-`/api` surface | `test_f41_the_allowlist_is_exactly_the_live_non_api_surface` | pass |
| Route order, authorization matrix, both completeness tests | — | pass, **unedited** |

Ten of these fail on the unmodified tree; the proof is `docs/gates/v0.7.4-phase-2.md` §3.

---

## 6. Authorization single-sourcing

| Test | Edited? | Result |
|---|---|---|
| `test_f8_audited_denied_single_source` | **no** | pass |
| `test_admin_only_is_derived_from_permissions_in_both_directions` | **no** | pass |
| `test_f28_no_role_comparison_outside_rbac` | **no** | pass |
| `test_every_api_route_is_in_the_permission_map` | **no** | pass |
| The generated authorization matrix (every route × every role) | **no** | pass |
| `test_every_unscoped_declaration_carries_a_written_justification` | assertions **no**; reads `rbac.tables.__file__` | pass — all five justifications found |
| `test_the_tables_are_re_exported_by_identity_not_by_copy` ×8 | **new** | pass |
| `test_no_module_but_tables_binds_an_authorization_table` | **new** | pass |

The two new tests were shown to fail against a deliberately-copying `__init__.py` and against a
`policy.py`-local fallback, under which **218 pre-existing tests pass green**
(`docs/gates/v0.7.4-phase-4.md` §2).

---

## 7. Import surface — from an installed wheel, in a clean virtualenv

Built wheel installed into a fresh Python 3.12 venv with **no source tree on the path**, run from
`/tmp`:

```
version: 0.7.4
rbac from: netcorenoc/rbac/__init__.py
  netcorenoc.rbac              14/14 resolve   missing=none
  netcorenoc.shaping           13/13 resolve   missing=none
  netcorenoc.varbind_profile    8/8  resolve   missing=none
identity in wheel: True
```

Every symbol in Phase 0's inventory resolves from its original module path, and the identity
property survives packaging.

```
$ python -m netcorenoc audit verify
audit chain OK: 0 entries verified; final hash 0000…0000
$ python -c "import netcorenoc.main, netcorenoc.runner; from netcorenoc.api import create_app"
netcorenoc.main imports OK; has main(): True
create_app importable from the wheel: True
```

---

## 8. Guards

```
DEBT_ALLOWLIST                {}
ALLOWLIST_MEMBERSHIP_CEILING  set()
COHESION_EXEMPT               ['engine.py']  ceiling={'engine.py': 542}
layer EXEMPTIONS              {}
PLANNED_THIS_RELEASE          set()
```

`DEBT_ALLOWLIST` is empty **and still defended in both directions**: `test_allowlist_only_shrinks`
(a stale entry) and `test_no_module_may_join_the_allowlist` (a new one). With the membership ceiling
also empty, *any* addition fails — which is the half that matters once the allowlist is empty, and
why the ceiling was emptied rather than left holding three dead names.

`COHESION_EXEMPT` is unchanged at one entry, with no owner and no fix date, and all five of its
constraints pass. The documentation-consistency guard is green (19 tests).

Modules over 400 lines: **`engine.py` (542) only**, cohesion-exempt by documented design.

---

## 9. Packaging

Wheel **and** sdist, both freshly built:

| Content | Wheel | Sdist |
|---|--:|--:|
| `netcorenoc/rbac/` | 3 | 3 |
| `netcorenoc/shaping/` | 4 | 4 |
| `varbind_accum.py` | 1 | 1 |
| `ui/` (full: index.html, app.js, style.css, vendor, .well-known) | 7 | 9 |
| `migrations/*.sql` | **7** | **7** |
| d3 `CHECKSUMS.txt` (F12/F13) | 1 | 1 |

```
$ make release-check
release-check OK: all sources agree on version 0.7.4
```

`[tool.setuptools.packages.find]` picks up `netcorenoc.rbac` and `netcorenoc.shaping` — confirmed
**from the built wheel**, not by reading the config.

---

## 10. Upgrade from a real v0.7.3 database

The v0.7.3 tree was extracted from its commit (`8b2c175`) and used to **write** the database — users,
devices driven through the real engine from real traps, situations, feedback, and two chained audit
rows through `audit.write_event`. The same file was then opened by the v0.7.4 **wheel** in the clean
virtualenv.

| | written by v0.7.3 | opened by v0.7.4 |
|---|---|---|
| `schema_version` | 7 (latest 7) | **7 (latest 7)** |
| users / NEs / situations | 4 / 3 / 2 | **4 / 3 / 2** |
| audit rows | 2 | **2** |
| audit final hash | `3d1bdf8a…79d9` | **`3d1bdf8a…79d9`** |
| store snapshot `sha256` | `28f636fd…2e96` | **`28f636fd…2e96`** |

```
$ NETCORENOC_DB=upgrade.db python -m netcorenoc audit verify
audit chain OK: 2 entries verified; final hash 3d1bdf8afd419a77ec6f263dddbd8ee2e8f402059c91d99d505710c6ffe179d9
```

**No migration ran. The store snapshot is byte-identical. The audit chain verifies to the same final
hash.**

---

## 11. Deployment

```
$ docker compose config
compose config VALID
```

**The Docker daemon is not available in this environment**, so the image was not built and the
container was not run. Stated plainly rather than papered over. The build prompt's specified
equivalent was performed instead: **the wheel, in a clean virtualenv, serving every public path with
the security headers intact.**

```
$ NETCORENOC_DB=… NETCORENOC_HTTP_PORT=18080 python -m netcorenoc.main
NetCoreNOC bootstrap admin created (first run) … shown ONCE
listening for traps on 0.0.0.0:11162/udp
web UI and API on http://0.0.0.0:18080/
```

| path | status | bytes | content-type |
|---|--:|--:|---|
| `/healthz` | 200 | 33 | application/json |
| `/readyz` | 200 | 18 | application/json |
| `/` | 200 | 5 818 | text/html; charset=utf-8 |
| `/openapi.json` | 200 | 28 609 | application/json |
| `/app.js` | 200 | 45 787 | application/javascript |
| `/style.css` | 200 | 13 251 | text/css; charset=utf-8 |
| `/vendor/d3.v7.min.js` | 200 | 279 706 | application/javascript |
| `/.well-known/security.txt` | 200 | 560 | text/plain; charset=utf-8 |

All eight — exactly `UNAUTHENTICATED_PATHS`, no more and no fewer. Security headers on the public
surface:

```
content-security-policy   PRESENT   default-src 'none'; script-src 'self'; style-src 'self'; …
x-content-type-options    PRESENT   nosniff
x-frame-options           PRESENT   DENY
referrer-policy           PRESENT   no-referrer
```

That is **all four** headers `perimeter.SECURITY_HEADERS` defines — the set is unchanged from
v0.7.3. (An initial check also probed `Permissions-Policy`; the project has never set it, so its
absence is not a regression. Recorded so the reading is not mistaken for one.)

```
GET /api/stats anonymous -> 401
```

The authenticated surface still refuses. Bootstrap still works from a wheel on first run.

---

## 12. Toolchain

| Gate | Result |
|---|---|
| `ruff check .` | All checks passed! |
| `ruff format --check .` | 238 files already formatted |
| `mypy --strict` | Success: no issues found in **121** source files |
| `vulture` (dead-code gate) | clean |
| `bandit` | clean — only the four pre-existing `nosec B608` "no failed test" warnings at `store/retention.py:23,27,31,35`, which are on the ROADMAP and untouched because this release changes no SQL |
| `pip-audit` | **No known vulnerabilities found** |
| documentation link-check | 72 passed |
| GitHub-Actions SHA-pin lint | 7 passed |
| structure guard | passed |
| architecture guard | 64 passed |
| layer guard | 13 passed |
| declaration gate | 69 passed |
| documentation-consistency guard | 19 passed |
| **coverage** | **95.81 %** — at or above the v0.7.3 figure of 95.80 % |

Test count: **701** (v0.7.3) → **754**. Added: 19 documentation guard, 19 F40/F41 regression, 9
authorization single-sourcing, 6 module-resolution.

---

## Gate 5 checklist

- [x] 1. Function parity — **56/56** hash identically
- [x] 2. Test parity — 19 deletions, every one listed and justified; **no assertion edited**
- [x] 3. Eval parity — **byte-identical**
- [x] 4. Earlier proofs intact — route order, handler hashes, `Store` hashes, concurrency, layer rule
- [x] 5. The gate — every registration path caught; every served public path still accepted
- [x] 6. Authorization single-sourcing — named, unedited, passing, plus two new tests proved by sabotage
- [x] 7. Import surface — every symbol resolves from an installed wheel in a clean virtualenv
- [x] 8. Guards — `DEBT_ALLOWLIST` empty and defended; `COHESION_EXEMPT` unchanged
- [x] 9. Packaging — wheel **and** sdist complete; `release-check` agrees on `0.7.4`
- [x] 10. Upgrade — real v0.7.3 DB, no migration, identical snapshot, same audit final hash
- [x] 11. Deployment — compose config valid; **no Docker daemon, stated plainly**; wheel equivalent verified
- [x] 12. Toolchain clean; coverage **95.81 % ≥ 95.80 %**

**Gate 5: PASSED.** Proceed to Phase 6.
