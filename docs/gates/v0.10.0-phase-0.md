# v0.10.0 — Gate 0

**The fence.** No v0.10.0 code exists until this gate closes. The reason is the release's whole
claim to honesty: *a plan ratified after implementation began is not a pre-registration.* Four
things happen here and nothing else — the §10 correction, F48, the tag recovery plan, and the
ratification itself, in that order, because the first three are prerequisites of the fourth
(`../analysis/PREREGISTRATION-0.10.0.md` §10).

**Baseline confirmed before anything was touched**, on the shipped v0.9.2 tree:

| Item | Command | Expected | Observed |
|---|---|---|---|
| test count | `pytest --collect-only -q` | 1086 | **1086** |
| `mypy --strict` | `python -m mypy` | clean, 146 files | **clean, 146 files** |
| ADR high-water mark | last `## N.` in `../adr/DECISIONS.md` | #139 | **#139** |
| migrations | `ls src/netcorenoc/migrations/` | `0001`–`0011` | **`0001`–`0011`** |
| `eval` output hash | `python eval/harness.py \| sha256sum` | `c2e8a0ce…8b9b6f26` | **`c2e8a0ce…8b9b6f26`** |
| `shadow_eval.py` | `wc -l` | 394 of 400 | **394** |
| `engine.py` | `wc -l` | 569 of 580 | **569** |
| runtime dependencies | `pyproject.toml` | 5 | **5** |
| version | `pyproject.toml` | 0.9.2 | **0.9.2** |

---

## 1. `EVIDENCE-BOUNDARY-0.9.2.md` §10 — the arithmetic, enumerated

### 1.1 What was published, and what is true

§10 declines to store a count of *unobservable asserted pairs* on the grounds that the surviving
columns already bound it. It published the bound as

```
observable asserted pairs ∈ [(m − b)·(n − m − (h − b)),  (m − b)·(n − m)]
```

and stated the measured case (`n = 6, m = 2, h = 3, b = 1`) as `[2, 2]`.

**Two things are wrong with that sentence, and they are different mistakes.**

1. **The upper expression is spurious.** `(m − b)·(n − m)` is the count that would obtain if the
   remainder held *no* hidden members — the special case `h = b` — not an attainable maximum given
   `h`. Once `h` and `b` are both known, the hidden count in the remainder is not a free parameter.
2. **The measured case is misstated.** Under the interval as published it evaluates to `[2, 4]`,
   not `[2, 2]`. The exact value is **2**.

The lower expression is **exact**, and the argument is one line: the hidden set `H` is partitioned
by the marked set `M` into the `b` members that are marked and the `h − b` that are not; every
member that is not marked is in the remainder; therefore `h − b` *is* the number of hidden members
in the remainder rather than a ceiling on it. The remainder holds exactly `(n − m) − (h − b)`
visible members, and a marked-by-remainder pair is observable exactly when both ends are visible:

```
observable asserted pairs = (m − b) · ((n − m) − (h − b))
```

### 1.2 The enumeration

Every non-empty marked set against every hidden set, for `n` from 2 to 8 — **86 868**
configurations. Truth counted the long way, over the actual pairs, sharing no arithmetic with
either expression under test.

Run first as a throwaway script, then committed as the test below so it re-runs on every commit
rather than living in this document as a quoted number:

```
configurations enumerated : 86868
lower expression == truth : 86868  (disagreements: 0)
upper expression == truth : 18658  (21.5%)
first disagreement of the lower expression: None

the case cited by section 10 (n=6, m=2, h=3, b=1):
  interval as published : [2, 4]
  published as          : [2, 2]
```

`86 868 = Σ(n=2..8) (2ⁿ − 1)·2ⁿ` — the `2ⁿ − 1` non-empty marked sets against all `2ⁿ` hidden sets.
The count is pinned by a test, so the figure quoted here and the figure the suite enumerates cannot
drift apart.

### 1.3 What changed, and the control that makes the test worth having

* `../architecture/EVIDENCE-BOUNDARY-0.9.2.md` §10 — the interval replaced by the exact expression,
  stated as exact rather than as a bound, with the superseded text quoted and named so a reader of
  the old sentence can find the correction.
* `../ROADMAP.md` — the same correction mirrored, with the same pointer.
* `tests/test_evidence_boundary_observable.py` — **new**, 4 tests, and the claim is now
  machine-checked on every run rather than asserted in prose.

**The correction makes §10's justification stronger.** "The surviving columns *bound* the quantity"
is a weak reason to omit a column; "the surviving columns *determine* it" means the omitted column
would have been strictly redundant.

**The control is the point of the file.** Appendix B's rule — *before trusting an invariant, ask
what value of its inputs would make it false* — exists in this repository because v0.9.2 found an
identity test that could never fail. So the suite also runs the **superseded** expression through
the same enumeration and requires it to *disagree*:

```
$ python -m pytest tests/test_evidence_boundary_observable.py -q
....                                                                     [100%]
4 passed in 0.93s
```

`test_the_superseded_upper_expression_disagrees` asserts the old formula is right in 21.0–22.0 % of
the space (measured: 21.5 %). Had it passed everywhere, the enumeration could not have distinguished
a correct expression from an incorrect one and the other three tests would have been worthless.

### 1.4 The propagation is wider than the brief believed — reported, not silently widened

The build prompt's Appendix B says this claim *"reached two documents"*, and §0.3 directs the
correction at two. **It reached six places.** Enumerated by search, with what was done to each:

| # | Location | Status |
|---|---|---|
| 1 | `../architecture/EVIDENCE-BOUNDARY-0.9.2.md` §10 | **corrected** — the normative statement |
| 2 | `../ROADMAP.md` (open-questions list) | **corrected** — a live document |
| 3 | `../security/SECURITY-REVIEW-0.9.2.md` §4.1 note 2 | **erratum appended**, as that file's new §7 |
| 4 | `v0.9.2-phase-1.md` §5 ("the bound is **exact** at `[2, 2]`") | **left intact** |
| 5 | `../releases/BUILD-REPORT-0.9.2.md`, *What this release does not claim* note 2 | **left intact** |
| 6 | `src/netcorenoc/migrations/0011_evidence_boundary.sql` comment | **left intact** |

**Why 4, 5 and 6 are not edited.** A gate document and a build report are *dated records of what was
believed at the time*; editing one retroactively is the failure the project's append-only
conventions exist to prevent, and it would also destroy the evidence that the mistake was made. An
applied migration is never touched (Part VII rule 2 and prime directive 7). Each is named here so
the correction is findable from any of them, and #3 gets an erratum rather than an edit because
v0.10.0 is appending F48 to that file anyway and leaving a wrong sentence in a document this
release is actively writing would be a different kind of failure.

**This is itself the Appendix B trap firing a third time.** The brief's own count of how far the
wrong sentence had spread was low by four.


---

## 2. F48 — the `source = 'server'` predicate, demonstrated

Issued in [`../security/SECURITY-REVIEW-0.9.2.md`](../security/SECURITY-REVIEW-0.9.2.md) §6, whose
§3 reserved the number. **F48 requires no production change** — the shipped code carries the
predicate in all three places it appears. It is a missing *demonstration*, not a missing *fix*, and
the argument for not inserting a fifth corrective release is
[DECISIONS #140](../adr/DECISIONS.md).

### 2.1 The reproduction — the shipped suite is blind to it

`AND m.source = 'server'` removed from **both halves** of `Store.reconciliation_drift`:

```diff
--- a/src/netcorenoc/store/feedback.py
+++ b/src/netcorenoc/store/feedback.py
@@ -197,13 +197,13 @@ class FeedbackMixin(StoreBase):
              "       f.excluded_reconciled_source AS source, ("
              "  SELECT COUNT(DISTINCT x.alarm_id) FROM feedback_exclusion x "
              "    JOIN feedback_member m ON m.feedback_id = x.feedback_id "
-            "     AND m.alarm_id = x.alarm_id AND m.source = 'server' "
+            "     AND m.alarm_id = x.alarm_id "
              "   WHERE x.feedback_id = f.id) AS recomputed "
              "FROM feedback f WHERE f.excluded_reconciled IS NOT NULL "
              "  AND f.excluded_reconciled <> ("
              "  SELECT COUNT(DISTINCT x.alarm_id) FROM feedback_exclusion x "
              "    JOIN feedback_member m ON m.feedback_id = x.feedback_id "
-            "     AND m.alarm_id = x.alarm_id AND m.source = 'server' "
+            "     AND m.alarm_id = x.alarm_id "
              "   WHERE x.feedback_id = f.id) "
              "ORDER BY f.id"
          )
```

```
$ python -m pytest -q --ignore=tests/test_evidence_boundary_f48.py \
                     --ignore=tests/test_evidence_boundary_observable.py
1086 passed in 267.34s (0:04:27)
```

**All 1086 of v0.9.2's tests pass with the predicate gone**, exactly as the brief states.

### 2.2 The non-equivalence proof

A guard against a change that alters no value proves nothing, so the two expressions are measured
against each other. The fixture is a **stale client view** — the case the v0.7.5 SSE teardown
produced routinely, not a hostile one: a three-member server bag, and a fourth real alarm the client
still believes is a member and marks.

| expression | value |
|---|---:|
| the live path — `excluded_reconciled`, stored at the verdict | **0** |
| an unfiltered join — the same query without the predicate | **1** |

```
{'feedback_id': 1, 'excluded_count': 1, 'stored': 0, 'source': 'live', 'recomputed': 1}
```

Verified non-equivalent. `excluded_count = 1` is the tier-1 report, recorded verbatim; the
reconciled count is 0 because the marked id names no member of the server's bag.

### 2.3 The guard, green

`tests/test_evidence_boundary_f48.py`, copied from the brief's Appendix D. Every symbol it depends
on resolved unchanged — `Member` (re-exported by `netcorenoc.engine` from `rootcause`),
`authutil.make_env`, `authutil.client_as`, and every column the fixture writes. **The tree had not
moved.**

```
$ python -m pytest tests/test_evidence_boundary_f48.py -q
...                                                                      [100%]
3 passed in 1.89s
```

**Three classes of deviation from verbatim, every one forced by a gate the brief itself requires to
be green, every one declared.** Appendix D as supplied cannot be committed to this tree: `make qa`
runs `ruff check .` and `mypy --strict`, and the file fails both.

**(a) `ruff check` — two lines over the 100-character limit** set by `pyproject.toml`:

```
E501 Line too long (101 > 100)  tests/test_evidence_boundary_f48.py:61:101
E501 Line too long (101 > 100)  tests/test_evidence_boundary_f48.py:120:101
```

Line 61's `INSERT INTO alarm_class …` SQL is split across two adjacent string literals — the
concatenated string is byte-identical — and line 120's one-line docstring is wrapped onto two.

**(b) `mypy --strict` — ten errors**, nine of them the same one:

```
tests/test_evidence_boundary_f48.py:19: error: Module "netcorenoc.engine" does not explicitly
    export attribute "Member"  [attr-defined]
tests/test_evidence_boundary_f48.py:53: error: Value of type "Row | None" is not indexable  [index]
    … (7 more [index], and one [arg-type] at line 102)
```

`aiosqlite`'s `fetchone()` is typed `Row | None`. **The sibling file `tests/test_evidence_boundary.py`
already carries `# type: ignore[index]` on exactly this construct, on four consecutive lines of an
almost identical fixture**, so the adjustment is the surrounding code's own idiom rather than an
invention.

**(c) the `Member` import.** Appendix D writes `from netcorenoc.engine import Member`. `engine.py`
imports the name from `rootcause` but does not re-export it, so `--strict` rejects the import.
**All four sibling test files that use `Member` import it from `netcorenoc.rootcause`** — its
defining module — and this file now does too. This is precisely the "the tree has genuinely moved
(a renamed helper)" case the brief permits: the appendix assumed a re-export that is not declared.

**None of the three touches an assertion, a fixture value, a bag, the `BACKFILL` constant or the
`UNFILTERED` control.** And that is checked rather than asserted: **both injections were re-run
after all three classes of change, and both still go red with the control green** (§2.4). A
"cosmetic" edit that quietly disarmed a guard is exactly the *never to make it pass more easily*
failure the brief warns about, so the guard was re-demonstrated rather than assumed to have
survived.

### 2.4 Both reds, and the control that passed under both

| injection | test that went red | control |
|---|---|---|
| the predicate removed from `Store.reconciliation_drift` (both halves) | `test_the_drift_check_reconciles_against_the_server_bag_only` | **passed** |
| the predicate removed from the pinned `BACKFILL` expression | `test_the_backfill_expression_reconciles_against_the_server_bag_only` | **passed** |

**Injection 1, verbatim:**

```
E       AssertionError: the drift check disagreed with the live path on a stale-view label, which
E       would send an operator hunting a write-path defect that does not exist
E       assert [{'feedback_i... 'live', ...}] == []
E         Full diff:
E         - []
E         + [
E         +     {
E         +         'feedback_id': 1,
E         +         'excluded_count': 1,
E         +         'stored': 0,
E         +         'source': 'live',
E         +         'recomputed': 1,
E         +     },
E         + ]
FAILED tests/test_evidence_boundary_f48.py::test_the_drift_check_reconciles_against_the_server_bag_only
========================= 1 failed, 2 passed in 1.79s ==========================
```

**Injection 2, verbatim:**

```
E       AssertionError: the backfill expression must agree with the live path
E       assert 1 == 0
FAILED tests/test_evidence_boundary_f48.py::test_the_backfill_expression_reconciles_against_the_server_bag_only
========================= 1 failed, 2 passed in 1.84s ==========================
```

**The control — `test_the_live_path_reconciles_against_the_server_bag_only` — is green in the
unmodified run and in both injected runs.** That is what establishes the fixture is a *stale-view*
fixture rather than a broken one, and that the two reds are about the two expressions rather than
about the label. Without it, "1 failed, 2 passed" would be consistent with a fixture that simply
stopped writing a label at all.

### 2.5 A third location, measured and left open

The brief names two injections. There is a **third** place the predicate appears — the migration
file itself — and **nothing in the tree guards it.** The test pins the backfill as a *constant*,
because a migration cannot be re-run against an already-migrated database, and nothing ties the
constant to `src/netcorenoc/migrations/0011_evidence_boundary.sql`. The test file's own docstring
asserts `tests/test_upgrade.py` catches that drift.

**It does not.** Measured rather than reasoned about — Appendix B's *"measuring nothing and
concluding CLOSED"* is on this repository's record:

```
# AND m.source = 'server' deleted from 0011_evidence_boundary.sql itself
$ python -m pytest tests/test_upgrade.py -q
7 passed in 2.15s
$ python -m pytest -q                      # the whole tree, both new files included
1093 passed in 270.29s (0:04:30)
```

The cause is specific: `test_upgrade.py`'s v0.9.1 fixture writes its exclusion through
`Store.add_feedback_exclusion` with **no client fingerprint**, so the label carries no
`feedback_member(source='client')` rows. With nothing on the untrusted side of the join, the
predicate has nothing to exclude and deleting it changes no value. The fixture is a correct upgrade
fixture and an **inert probe** for this predicate.

**Not closed here, deliberately.** Appendix D is copied verbatim and Gate 0 is fenced to four items;
Part VII rule 7 sends a newly revealed defect to a ROADMAP line and a finding number rather than
into an unrelated commit. Carried as **F49** for the v0.10.0 security review and as a
[`../ROADMAP.md`](../ROADMAP.md) line. *F48 closes two of the three locations and proves the third
is open.*

The migration was restored byte-for-byte after the probe, and the tree re-verified green.

---

## 3. The remote tags

[`../releases/TAG-RECOVERY.md`](../releases/TAG-RECOVERY.md) carries the plan. Two corrections to
the brief, both measured:

1. **`v0.9.2` now has a remote tag** — created since the brief was written, pointing at `5a9b39c`,
   the release-branch tip. Nothing to do, and it becomes this document's control.
2. **`v0.7.4` and `v0.7.5` are also missing**, which the brief does not mention. **Six** releases
   lack an immutable reference, not five, and the gap opens one release earlier than believed.

Every candidate is verified four ways — merge-commit tree against release-branch-tip tree, plus
`pyproject.toml`, `src/netcorenoc/__init__.py` and `CHANGELOG.md` at that commit. All seven checked
pairs are **IDENTICAL** with all three version declarations agreeing; the table is TAG-RECOVERY §2.

**The control (Appendix B: a probe with no control proves nothing).** `v0.9.2` was tagged by the
maintainer without this procedure, so it is an independent answer the method can be checked
against. The method predicts `v0.9.2`'s content is the tree shared by `0d92544` and `5a9b39c`; the
existing tag resolves to `5a9b39c` and the trees are identical. **The method agrees with an answer
it did not produce.**

It also exposes one real discrepancy, recorded rather than resolved silently: the maintainer's
existing tags point at the **release-branch tip**, and the brief instructs that recreated tags point
at the **merge commit**. Both capture byte-identical trees. TAG-RECOVERY §3 follows the brief, says
why, and gives the substitution for the other choice.

---

## 4. The pre-registration, ratified

### 4.1 The recorded hash

```
docs/analysis/PREREGISTRATION-0.10.0.md
sha256  c03aef0181554c0c71482e57d03677f25964c3a5ac20a7bf1b1d74bff1ba1e01
commit  6b1c73a59e1dafc312f02a4d60e58093f575093a
tag     v0.10.0-gate0  (annotated; cannot be pushed from here — TAG-RECOVERY §6)
```

```
$ git show --stat --oneline 6b1c73a | tail -3
6b1c73a docs(analysis): ratify the v0.10.0 pre-registration
 docs/analysis/PREREGISTRATION-0.10.0.md | 512 ++++++++++++++++++++++++++++++++
 1 file changed, 512 insertions(+)
```

**One file, 512 insertions, nothing else.** That is the requirement of the plan's own §11.1, and it
is what makes the ratified content a single addressable object rather than a region of a larger
diff.

### 4.2 The one permitted modification

Appendix C is copied **verbatim**. Exactly one line differs, and it is the line §0.1 names:

```diff
-**STATUS: DRAFT FOR RATIFICATION — revision 2.** This document becomes the pre-registration the
+**STATUS: RATIFIED — 2026-08-12.** This document becomes the pre-registration the
```

Nothing else was reworded, corrected, improved, added or renumbered. **That includes three places
where the plan is now demonstrably out of date**, and leaving them is the point rather than an
oversight:

* **§10.1** says removing the predicate "leaves 1086 tests green" — confirmed by measurement (§2.1);
* **§10.2** describes §10's interval as still uncorrected — it was corrected earlier in this same
  gate (§1);
* **§10.3** says the remote returns "only `v0.7.3`" — it now also returns `v0.9.2`, and the gap is
  six releases rather than the five listed (§3).

A pre-registration is a record of *what was decided in advance*, not a live document. Editing §10 to
match the world after closing its prerequisites would produce a plan that had never been wrong about
anything, which is precisely the artefact this discipline exists to prevent. The corrections live in
this gate; the plan keeps its date.

### 4.3 The guard, pinned beside v0.9.0's — both

`tests/test_preregistration.py` now guards **two** plans. v0.9.0's is not retired: it still governs
the corpus v0.10.0 reads, and a guard that migrated from one plan to the next would leave every
earlier release's standard of evidence unprotected the moment it stopped being current.

The hash lives in two files with different reasons to exist — the test constant and this gate — so
editing one quietly is an obviously deliberate diff.

Added beside the parametrised hash and gate checks:

* `test_both_plans_are_guarded` — **guards the guard.** Parametrising over a table introduces a new
  failure mode: a table that quietly lost an entry retires a plan's guard with nothing going red.
  The table's length and contents are asserted directly.
* `test_the_0_10_0_plan_keeps_the_third_value_and_the_unspent_seal` — deliberately **not** the same
  assertions as v0.9.0's structural test. The two plans register different things, so a shared check
  would have to be weak enough to pass both, which is how a guard becomes a formality. It asserts
  what v0.10.0 would be worthless without: `INSUFFICIENT_EVIDENCE` registered as a **terminal**
  third value, all three verdict names present, the seal registered as **constructed and not spent**,
  the query count registered at **0**, "No promotion", and the `incumbent_linked` invariant.

### 4.4 The disagreements between the brief and the plan, reported rather than resolved

Part IV requires that an apparent disagreement between the build document and the plan be **reported
in the Phase 1 gate**, not resolved silently. None found in Phase 0 rises to a contradiction: the
three §10 items above are the plan describing a world that its own prerequisites then changed, which
is the mechanism working. The full Part IV check belongs to Gate 1 and is recorded there.

---

## Gate 0 — closed

| Requirement | Evidence | State |
|---|---|---|
| F48 issued with both reds recorded | `../security/SECURITY-REVIEW-0.9.2.md` §6; §2.4 above | **met** |
| …and a control that passed under both | `test_the_live_path_…`, green in all three runs | **met** |
| §10 corrected | §1; `EVIDENCE-BOUNDARY-0.9.2.md` §10, `../ROADMAP.md` | **met** |
| …and machine-checked | `tests/test_evidence_boundary_observable.py`, 4 tests, with a control | **met** |
| `TAG-RECOVERY.md` written | `../releases/TAG-RECOVERY.md` | **met** |
| …every candidate verified by tree comparison | §3; seven pairs IDENTICAL, plus an independent control | **met** |
| the plan written verbatim | §4.2 — one line differs, and it is the permitted one | **met** |
| …committed alone | §4.1 — 1 file, 512 insertions | **met** |
| …hashed | `c03aef01…f1ba1e01` | **met** |
| …pinned by a test | `tests/test_preregistration.py`, both plans | **met** |
| …tagged | `v0.10.0-gate0` on `6b1c73a`, annotated | **met** |
| no v0.10.0 code exists | `src/` untouched; migrations still `0001`–`0011` | **met** |
| `make qa && make eval` green | below | **met** |

```
$ python -m ruff check . && python -m ruff format --check .
All checks passed!
336 files already formatted
$ python -m mypy
Success: no issues found in 148 source files
$ python -m vulture src/netcorenoc vulture_allowlist.py
$ python -m pytest -q
1098 passed in 265.87s (0:04:25)
$ python eval/harness.py | sha256sum
c2e8a0ced29d9edf986279d41089ddb68e18da65a46bdc7e9f04811e8b9b6f26  -
```

**`make eval` is byte-identical to v0.7.0 onward.** Nothing in Phase 0 touched a code path. The
delta is **1086 → 1098 tests**: 4 from `test_evidence_boundary_observable.py`, 3 from
`test_evidence_boundary_f48.py`, and 5 from `test_preregistration.py` growing from 4 tests to 9 as
it took on the second plan. **No file under `src/` changed at all.**

**Phase 1 may now begin**, and its first implementation commit cites
`c03aef0181554c0c71482e57d03677f25964c3a5ac20a7bf1b1d74bff1ba1e01`.
