__pycache__/
*.py[cod]
*.egg-info/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
coverage.xml
htmlcov/
dist/
build/
*.db
*.db-wal
*.db-shm
result
# Local environment / secrets for docker compose (only .env.example is committed).
.env
# Local TLS material referenced by the commented compose profile.
/tls/

# The testbed's runtime state: the phase file, the lab database, per-process logs (v0.17.0).
testbed/state/
testbed/logs/
