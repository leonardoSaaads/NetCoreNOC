"""The correlation decision, and the vocabulary it is made in.

Sliding-window correlation, incremental co-occurrence learning, the scoring seam and its
contract, temporal precedence, learned severity, and the varbind profiler. This domain
**imports no other domain** — everything else in `engine/` is downstream of it.
"""
