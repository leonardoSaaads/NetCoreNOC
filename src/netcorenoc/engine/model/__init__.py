"""The model family: the kinds, the fit they share, the artefact, and the dispatch.

Exact marginal Shapley attribution over the three link features, greedy CART and the
three kinds built on it, the challenger, the training decision, and the one module that
says which scorer a stored row describes.
"""
