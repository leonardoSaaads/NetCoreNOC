"""Whether a challenger is better, and whether it may be promoted.

Shadow mode's lifecycle, the grouped cross-validated estimator, the admission filter, the
assertion check, the judge's three-valued verdict, the folds an evaluation stores, and the
promotion gate that re-derives all of it server-side.
"""
